import React, { } from 'react';


const Letter = () => {
 

  return (
    <div>
       {/* <section id="team-letter" className="section section-alt">
        <h2>Technician Letter Builder</h2>
        <p className="section-text">
          Build a clean service-visit letter with site details, approvals, and addresses that can be
          copied into email or printed for the customer.
        </p>
        <div className="letter-layout">
          <form
            className="letter-form"
            onSubmit={(e) => {
              e.preventDefault();
            }}
          >
            <h3>Letter Details</h3>
            <input
              type="text"
              placeholder="Client name"
              value={clientName}
              onChange={(e) => setClientName(e.target.value)}
            />
            <input
              type="text"
              placeholder="Building name"
              value={buildingName}
              onChange={(e) => setBuildingName(e.target.value)}
            />
            <input
              type="text"
              placeholder="Address / Street"
              value={address}
              onChange={(e) => setAddress(e.target.value)}
            />
            <input
              type="text"
              placeholder="City"
              value={city}
              onChange={(e) => setCity(e.target.value)}
            />
            <input
              type="date"
              placeholder="Visit date"
              value={visitDate}
              onChange={(e) => setVisitDate(e.target.value)}
            />
            <input
              type="text"
              placeholder="Technician name"
              value={technicianName}
              onChange={(e) => setTechnicianName(e.target.value)}
            />
          </form>

          <div className="letter-preview">
            <h3>Letter Preview</h3>
            <textarea value={letterPreview} readOnly rows={18} />
          </div>
        </div>
      </section> */}
            </div>
  );
};

export default Letter 
